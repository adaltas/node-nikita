{
  "config": {
    "type": "object",
    "properties": {
      "encoding": {
        "type": "string",
        "default": "utf8",
        "description": "File encoding."
      },
      "parse": {
        "typeof": "function",
        "description": "User-defined function to parse the content from ini format, default to\n`require('ini').parse`, see\n'nikita.file.utils.ini.parse_multi_brackets'. "
      },
      "target": {
        "type": "string",
        "description": "Path to a ini file to read."
      }
    },
    "required": [
      "target"
    ]
  }
}
