{
  "config": {
    "type": "object",
    "properties": {
      "cn": {
        "type": "string",
        "description": "Name of the group to delete."
      },
      "connection": {
        "type": "object",
        "$ref": "module://@nikitajs/network/http#/definitions/config",
        "required": [
          "principal",
          "password"
        ]
      }
    },
    "required": [
      "cn",
      "connection"
    ]
  }
}
