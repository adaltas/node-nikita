{
  "config": {
    "type": "object",
    "allOf": [
      {
        "properties": {
          "divider": {
            "type": "string",
            "default": " : ",
            "description": "The characters used to join the hierarchy of headers to create a markdown header."
          },
          "enter": {
            "type": "boolean",
            "default": true,
            "description": "Enable or disable the entering messages."
          },
          "serializer": {
            "type": "object",
            "default": {},
            "description": "Internal property, expose access to the serializer object passed to the `log.fs` action."
          }
        }
      },
      {
        "$ref": "module://@nikitajs/log/fs#/definitions/config"
      }
    ]
  }
}
