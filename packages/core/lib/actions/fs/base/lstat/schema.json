{
  "config": {
    "type": "object",
    "properties": {
      "target": {
        "type": "string",
        "description": "Location of the file from where to obtain information."
      }
    },
    "required": [
      "target"
    ]
  }
}
