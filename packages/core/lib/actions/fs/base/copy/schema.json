{
  "config": {
    "type": "object",
    "properties": {
      "source": {
        "type": "string",
        "description": "Source file to be copied."
      },
      "target": {
        "type": "string",
        "description": "Destination file where to copy the source file."
      }
    },
    "required": [
      "source",
      "target"
    ]
  }
}
