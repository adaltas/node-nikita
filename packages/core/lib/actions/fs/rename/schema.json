{
  "config": {
    "type": "object",
    "properties": {
      "source": {
        "type": "string",
        "description": "Location of the file to rename."
      },
      "target": {
        "type": "string",
        "description": "New name of the file."
      }
    },
    "required": [
      "source",
      "target"
    ]
  }
}
