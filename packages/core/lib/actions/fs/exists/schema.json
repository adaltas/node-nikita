{
  "config": {
    "type": "object",
    "properties": {
      "target": {
        "type": "string",
        "description": "Destination file to check existance."
      }
    },
    "required": [
      "target"
    ]
  }
}
