{
  "config": {
    "type": "object",
    "properties": {
      "gid": {
        "$ref": "module://@nikitajs/core/actions/fs/base/chown#/definitions/config/properties/gid"
      },
      "stats": {
        "typeof": "object",
        "description": "Stat object of the target file. Short-circuit to avoid fetching the\nstat object associated with the target if one is already available."
      },
      "target": {
        "type": "string",
        "description": "Location of the file which permissions will change."
      },
      "uid": {
        "$ref": "module://@nikitajs/core/actions/fs/base/chown#/definitions/config/properties/uid"
      }
    },
    "required": [
      "target"
    ]
  }
}
