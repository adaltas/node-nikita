// Generated by CoffeeScript 2.4.1
var Validator;

({Validator} = require('jsonschema'));

module.exports = function() {
  var validator;
  validator = new Validator();
  return {
    add: function(name, schema) {
      if (!schema) {
        return;
      }
      return validator.addSchema(schema, name);
    },
    validate: function(options, schema) {
      return validator.validate(options, schema);
    },
    list: function() {
      return validator.schemas;
    }
  };
};
