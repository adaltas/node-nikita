{
  "config": {
    "type": "object",
    "properties": {
      "containers": {
        "$ref": "module://@nikitajs/lxd/cluster#/definitions/config/properties/containers"
      },
      "wait": {
        "type": "boolean",
        "default": false,
        "description": "Wait for containers to be stopped before finishing action"
      },
      "pre_stop": {
        "typeof": "function"
      }
    },
    "required": [
      "containers"
    ]
  }
}
