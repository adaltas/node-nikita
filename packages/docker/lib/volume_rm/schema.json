{
  "config": {
    "type": "object",
    "properties": {
      "docker": {
        "$ref": "module://@nikitajs/docker/tools/execute#/definitions/docker"
      },
      "name": {
        "type": "string",
        "description": "Specify volume name."
      }
    },
    "required": ["name"]
  }
}
