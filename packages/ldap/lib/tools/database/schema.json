{
  "config": {
    "type": "object",
    "allOf": [
      {
        "properties": {
          "base": {
            "const": "cn=config",
            "default": "cn=config"
          },
          "suffix": {
            "type": "string",
            "description": "The suffix associated with the database."
          }
        },
        "required": [
          "suffix"
        ]
      },
      {
        "$ref": "module://@nikitajs/ldap/search"
      }
    ]
  }
}
