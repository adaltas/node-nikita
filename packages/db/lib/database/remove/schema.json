{
  "config": {
    "type": "object",
    "$ref": "module://@nikitajs/db/query#/definitions/db",
    "properties": {
      "database": {
        "type": "string",
        "description": "The name of the database to remove."
      }
    },
    "required": [
      "database"
    ]
  }
}
