{
  "config": {
    "type": "object",
    "allOf": [
      {
        "$ref": "module://@nikitajs/db/lib/query#/definitions/db"
      }
    ],
    "properties": {
      "database": {
        "type": "string",
        "description": "The name of the database to wait for."
      }
    },
    "required": [
      "database"
    ]
  }
}
