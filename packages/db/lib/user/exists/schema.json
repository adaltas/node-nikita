{
  "config": {
    "type": "object",
    "$ref": "module://@nikitajs/db/query#/definitions/db",
    "properties": {
      "username": {
        "type": "string",
        "description": "Name of the user to check for existance."
      }
    },
    "required": [
      "username"
    ]
  }
}
