{
  "config": {
    "type": "object",
    "allOf": [
      {
        "$ref": "module://@nikitajs/db/lib/query#/definitions/db"
      }
    ],
    "properties": {
      "database": {
        "type": "string",
        "description": "The database name storing the schemas."
      }
    },
    "required": [
      "database"
    ]
  }
}
