{
  "config": {
    "type": "object",
    "properties": {
      "admin": {
        "$ref": "module://@nikitajs/krb5/execute#/definitions/config/properties/admin"
      },
      "keytab": {
        "type": "string",
        "description": "Path to the file storing key entries."
      },
      "principal": {
        "type": "string",
        "description": "Principal to be created."
      },
      "realm": {
        "type": "string",
        "description": "The realm the principal belongs to."
      }
    },
    "required": [
      "principal"
    ]
  }
}
