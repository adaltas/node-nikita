{
  "config": {
    "type": "object",
    "$ref": "module://@nikitajs/incus/cluster#/definitions/config",
    "properties": {
      "wait": {
        "type": "boolean",
        "default": false,
        "description": "Wait for containers to be stopped before finishing action"
      },
      "pre_stop": {
        "typeof": "function"
      }
    },
    "required": [
      "containers"
    ]
  }
}
