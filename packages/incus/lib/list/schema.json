{
  "config": {
    "type": "object",
    "properties": {
      "type": {
        "type": "string",
        "enum": ["container", "virtual-machine"],
        "description": "Display only one type of instances."
      }
    }
  }
}
