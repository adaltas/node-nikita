{
  "config": {
    "type": "object",
    "properties": {
      "name": {
        "$ref": "module://@nikitajs/incus/network#/definitions/config/properties/name",
        "description": "The network name to check."
      }
    },
    "required": ["name"]
  }
}
