{
  "config": {
    "type": "object",
    "properties": {
      "network": {
        "type": "string",
        "description": "Name of the network to delete."
      }
    },
    "required": [
      "network"
    ]
  }
}
