{
  "config": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "The operation name to create."
      }
    },
    "required": ["name"]
  }
}
