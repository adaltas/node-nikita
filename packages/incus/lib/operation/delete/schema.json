{
  "config": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "The operation name to delete."
      }
    },
    "required": ["name"]
  }
}
