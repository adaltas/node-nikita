// Generated by CoffeeScript 2.5.1
// Nikita

// This is the main Nikita entry point. It expose a function to initialize a new
// Nikita session.

// Dependencies
var conditions, create, schema, session;

require('./register');

conditions = require('./plugins/conditions');

schema = require('./plugins/schema');

session = require('./session');

create = function() {
  return session({
    plugins: [conditions, schema]
  }, ...arguments);
};

// Source Code
module.exports = new Proxy(create, {
  get: function(target, name) {
    return create()[name];
  }
});
