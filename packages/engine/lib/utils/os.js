// Generated by CoffeeScript 2.5.1
var path;

path = require('path');

module.exports = {
  whoami: function({ssh, platform = process.platform} = {}) {
    if (ssh) {
      return ssh.config.username;
    }
    if (/^win/.test(platform)) {
      return process.env['USERPROFILE'].split(path.win32.sep)[2];
    }
    if (process.env['USER']) {
      return process.env['USER'];
    }
    if (/^\/root$/.test(process.env['HOME'])) {
      return process.env['HOME'].split('/')[1];
    }
    if (/^\/home\/[^\/]+$/.test(process.env['HOME'])) {
      return process.env['HOME'].split('/')[2];
    }
  }
};
