// Generated by CoffeeScript 2.1.1
// # `nikita.ssh(options)`

// Get the ssh connection if any.

// ## Options

// * `ssh` (boolean)   
//   Return the SSH connection if any and if true, null if false.

// ## Source code
module.exports = {
  get: true,
  handler: function(options) {
    if (options.ssh == null) {
      options.ssh = options.argument;
    }
    if ((options.ssh != null) && !typeof options.ssh === 'boolean') {
      throw Error(`Invalid Option: ssh must be a boolean value or null or undefined, got ${JSON.stringify(options.ssh)}`);
    }
    if (options.ssh === true && !options.store.ssh) {
      throw Error('Unavailable Connection: requested to return a SSH connection but none is initialized');
    }
    if (options.ssh === false) {
      return null;
    }
    return options.store.ssh;
  }
};
