// Generated by CoffeeScript 1.10.0
var docker;

module.exports = function(options, callback) {
  var cmd;
  if (options.image == null) {
    return callback(Error('Missing image parameter'));
  }
  if (options.tag == null) {
    options.tag = 'latest';
  }
  cmd = " images | grep '" + options.image + "' | grep '" + options.tag + "' | awk ' { print $3 }'";
  options.log({
    message: "Getting image cheksum :" + options.image,
    level: 'INFO',
    module: 'mecano/src/docker/checksum'
  });
  return docker.exec(cmd, options, true, (function(_this) {
    return function(err, executed, stdout, stderr, cheksum) {
      var checksum;
      if (!executed) {
        options.log({
          message: "Image does not exist :" + options.image,
          level: 'INFO',
          module: 'mecano/src/docker/checksum'
        });
      }
      checksum = stdout === '' ? false : stdout.toString().trim();
      if (executed) {
        options.log({
          message: "Image found : " + options.image + " with checksum: " + checksum,
          level: 'INFO',
          module: 'mecano/src/docker/checksum'
        });
      }
      return callback(err, executed, stdout, stderr, checksum);
    };
  })(this));
};

docker = require('../misc/docker');
