// Generated by CoffeeScript 1.10.0
var docker;

module.exports = function(options, callback) {
  var cmd;
  if (options.repository == null) {
    return callback(Error('Missing repository parameter'));
  }
  if (options.tag == null) {
    options.tag = 'latest';
  }
  cmd = " images | grep '" + options.repository + "' | grep '" + options.tag + "' | awk ' { print $3 }'";
  options.log({
    message: "Getting repository cheksum :" + options.repository,
    level: 'INFO',
    module: 'mecano/lib/docker/checksum'
  });
  return docker.exec(cmd, options, true, (function(_this) {
    return function(err, executed, stdout, stderr, cheksum) {
      var checksum;
      if (!executed) {
        options.log({
          message: "Image does not exist :" + options.repository,
          level: 'INFO',
          module: 'mecano/lib/docker/checksum'
        });
      }
      checksum = stdout === '' ? false : stdout.toString().trim();
      if (executed) {
        options.log({
          message: "Image found : " + options.repository + " with checksum: " + checksum,
          level: 'INFO',
          module: 'mecano/lib/docker/checksum'
        });
      }
      return callback(err, executed, stdout, stderr, checksum);
    };
  })(this));
};

docker = require('../misc/docker');
