// Generated by CoffeeScript 1.10.0
module.exports = function(options, callback) {
  var k, ref, v;
  if (options.docker == null) {
    options.docker = {};
  }
  ref = options.docker;
  for (k in ref) {
    v = ref[k];
    if (options[k] == null) {
      options[k] = v;
    }
  }
  if (options.detach == null) {
    options.detach = true;
  }
  if (options.rm == null) {
    options.rm = false;
  }
  if (!((options.name != null) || (options.container != null))) {
    return callback(Error('Missing container name'));
  }
  return this.docker_run(options, callback);
};
