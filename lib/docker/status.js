// Generated by CoffeeScript 1.10.0
var docker, util;

module.exports = function(options, callback) {
  var cmd;
  if (options.container == null) {
    return callback(Error('Missing container parameter'));
  }
  cmd = " ps --filter status=running | grep '" + options.container + "' ";
  return docker.exec(cmd, options, true, function(err, running, stdout, stderr) {
    return callback(err, running, stdout, stderr);
  });
};

docker = require('../misc/docker');

util = require('util');
