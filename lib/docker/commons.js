// Generated by CoffeeScript 1.10.0
var mecano;

mecano = require('..');

module.exports = {
  get_options: function(options) {
    var exec_opts, i, k, len, ref;
    exec_opts = [];
    ref = ['ssh', 'log', 'stdout', 'stderr', 'cwd', 'code', 'code_skipped', 'machine', 'force'];
    for (i = 0, len = ref.length; i < len; i++) {
      k = ref[i];
      if (options[k] != null) {
        exec_opts[k] = options[k];
      }
    }
    return exec_opts;
  }
};
