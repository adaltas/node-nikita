// Generated by CoffeeScript 2.3.0
var ssh;

module.exports = ssh = {
  compare: function(ssh1, ssh2) {
    var compare_config;
    // Between 2 configurations
    compare_config = function(config1, config2) {
      return config1 && config2 && config1.host === config2.host && (config1.port || 22) === (config2.port || 22) && config1.username === config2.username;
    };
    // 2 null
    // Compare
    // 2 SSH instances
    // 2 SSH configurations
    // SSH instance with SSH configuration
    // SSH instance with SSH configuration
    return (!ssh1 && !ssh2 && !!ssh1 === !!ssh2) || (ssh.is(ssh1) && ssh.is(ssh2) && compare_config(ssh1.config, ssh2.config)) || (!ssh.is(ssh1) && !ssh.is(ssh2) && compare_config(ssh1, ssh2)) || (ssh.is(ssh1) && !ssh.is(ssh2) && compare_config(ssh1.config, ssh2)) || (!ssh.is(ssh1) && ssh.is(ssh2) && compare_config(ssh1, ssh2.config)) || false;
  },
  is: function(ssh) {
    var ref, ref1;
    if ((ssh != null ? (ref = ssh._sshstream) != null ? (ref1 = ref.config) != null ? ref1.ident : void 0 : void 0 : void 0) == null) {
      return false;
    }
    return ssh._sshstream.config.ident === 'SSH-2.0-ssh2js0.2.0';
  }
};
