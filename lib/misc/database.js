// Generated by CoffeeScript 1.10.0
var mecano;

mecano = require('..');

module.exports.wrap = function(options) {
  var cmd;
  if (options == null) {
    options = {};
  }
  if (options.engine == null) {
    options.engine = 'POSTGRES';
  }
  options.engine = options.engine.toUpperCase();
  if (!((options.name != null) || (options.password != null))) {
    return Error('Mssing name/password');
  }
  cmd = '';
  switch (options.engine) {
    case 'MYSQL':
      cmd += "mysql -h " + options.host + " -u " + options.name + " -p " + options.password + " ";
      if (options.database) {
        cmd += "-d " + db;
      }
      return cmd;
      break;
    case 'POSTGRES':
      cmd += "PGPASSWORD=" + options.password + " psql -h " + options.host + " -U " + options.name + " ";
      if (options.database) {
        cmd += "-d " + db;
      }
      return cmd;
      break;
  }
  return cmd;
};
