// Generated by CoffeeScript 1.10.0
var each, exec, fs, misc, template;

module.exports = {
  "if": function(options, skip, succeed) {
    var ok;
    if (!Array.isArray(options["if"])) {
      options["if"] = [options["if"]];
    }
    ok = true;
    return each(options["if"]).run((function(_this) {
      return function(si, next) {
        var err, error, type;
        if (!ok) {
          return next();
        }
        type = typeof si;
        if (si === null || type === 'undefined') {
          ok = false;
          return next();
        } else if (type === 'boolean' || type === 'number') {
          if (!si) {
            ok = false;
          }
          return next();
        } else if (type === 'function') {
          if (si.length < 2) {
            try {
              if (!si.call(_this, options)) {
                ok = false;
              }
              next();
            } catch (error) {
              err = error;
              next(err);
            }
          }
          if (si.length === 2) {
            return si.call(_this, options, function(err, is_ok) {
              if (err) {
                return next(err);
              }
              if (!is_ok) {
                ok = false;
              }
              return next();
            });
          } else {
            return next(new Error("Invalid callback"));
          }
        } else if (type === 'string') {
          si = template(si, options);
          if (si.length === 0) {
            ok = false;
          }
          return next();
        } else {
          return next(new Error("Invalid condition type"));
        }
      };
    })(this)).then(function(err) {
      if (err || !ok) {
        return skip(err);
      } else {
        return succeed();
      }
    });
  },
  unless: function(options, skip, succeed) {
    var ok;
    if (!Array.isArray(options.unless)) {
      options.unless = [options.unless];
    }
    ok = true;
    return each(options.unless).run((function(_this) {
      return function(not_if, next) {
        var err, error, type;
        if (!ok) {
          return next();
        }
        type = typeof not_if;
        if (not_if === null || type === 'undefined') {
          ok = true;
          return next();
        } else if (type === 'boolean' || type === 'number') {
          if (not_if) {
            ok = false;
          }
          return next();
        } else if (type === 'function') {
          if (not_if.length < 2) {
            try {
              if (not_if.call(_this, options)) {
                ok = false;
              }
              next();
            } catch (error) {
              err = error;
              next(err);
            }
          }
          if (not_if.length === 2) {
            return not_if.call(_this, options, function(err, is_ok) {
              if (err) {
                return next(err);
              }
              if (is_ok) {
                ok = false;
              }
              return next();
            });
          } else {
            return next(new Error("Invalid callback"));
          }
        } else if (type === 'string') {
          not_if = template(not_if, options);
          if (not_if.length !== 0) {
            ok = false;
          }
          return next();
        } else {
          return next(new Error("Invalid condition type"));
        }
      };
    })(this)).then(function(err) {
      if (err || !ok) {
        return skip(err);
      } else {
        return succeed();
      }
    });
  },
  if_exec: function(options, skip, succeed) {
    return each(options.if_exec).run(function(cmd, next) {
      var run;
      if (typeof options.log === "function") {
        options.log("Mecano `if_exec`: " + cmd);
      }
      options = {
        cmd: cmd,
        ssh: options.ssh
      };
      run = exec(options);
      if (options.stdout) {
        run.stdout.pipe(options.stdout, {
          end: false
        });
      }
      if (options.stderr) {
        run.stderr.pipe(options.stderr, {
          end: false
        });
      }
      return run.on("exit", function(code) {
        if (typeof options.log === "function") {
          options.log("Mecano `if_exec`: code is \"" + code + "\"");
        }
        if (code === 0) {
          return next();
        } else {
          return skip();
        }
      });
    }).then(succeed);
  },
  unless_exec: function(options, skip, succeed) {
    return each(options.unless_exec).run(function(cmd, next) {
      var run;
      if (typeof options.log === "function") {
        options.log("Mecano `unless_exec`: " + cmd);
      }
      options = {
        cmd: cmd,
        ssh: options.ssh
      };
      run = exec(options);
      if (options.stdout) {
        run.stdout.pipe(options.stdout, {
          end: false
        });
      }
      if (options.stderr) {
        run.stderr.pipe(options.stderr, {
          end: false
        });
      }
      return run.on("exit", function(code) {
        if (typeof options.log === "function") {
          options.log("Mecano `unless_exec`: code is \"" + code + "\"");
        }
        if (code === 0) {
          return skip();
        } else {
          return next();
        }
      });
    }).then(succeed);
  },
  if_exists: function(options, skip, succeed) {
    var destination, if_exists, ssh;
    ssh = options.ssh, if_exists = options.if_exists, destination = options.destination;
    if (typeof if_exists === 'boolean' && destination) {
      if_exists = if_exists ? [destination] : null;
    }
    return each(if_exists).run(function(if_exists, next) {
      return fs.exists(ssh, if_exists, function(err, exists) {
        if (exists) {
          return next();
        } else {
          return skip();
        }
      });
    }).then(succeed);
  },
  unless_exists: function(options, skip, succeed) {
    var destination, ssh, unless_exists;
    ssh = options.ssh, unless_exists = options.unless_exists, destination = options.destination;
    if (typeof unless_exists === 'boolean' && destination) {
      unless_exists = unless_exists ? [destination] : null;
    }
    return each(unless_exists).run(function(unless_exists, next) {
      return fs.exists(ssh, unless_exists, function(err, exists) {
        if (exists) {
          return skip();
        } else {
          return next();
        }
      });
    }).then(succeed);
  },
  not_if: function(options, skip, succeed) {
    var ok;
    console.log('not_if is deprecated, use unless');
    options.unless = options.not_if;
    delete options.not_if;
    if (!Array.isArray(options.unless)) {
      options.unless = [options.unless];
    }
    ok = true;
    return each(options.unless).run((function(_this) {
      return function(not_if, next) {
        var err, error, type;
        if (!ok) {
          return next();
        }
        type = typeof not_if;
        if (not_if === null || type === 'undefined') {
          ok = true;
          return next();
        } else if (type === 'boolean' || type === 'number') {
          if (not_if) {
            ok = false;
          }
          return next();
        } else if (type === 'function') {
          if (not_if.length < 2) {
            try {
              if (not_if.call(_this, options)) {
                ok = false;
              }
              next();
            } catch (error) {
              err = error;
              next(err);
            }
          }
          if (not_if.length === 2) {
            return not_if.call(_this, options, function(err, is_ok) {
              if (err) {
                return next(err);
              }
              if (is_ok) {
                ok = false;
              }
              return next();
            });
          } else {
            return next(new Error("Invalid callback"));
          }
        } else if (type === 'string') {
          not_if = template(not_if, options);
          if (not_if.length !== 0) {
            ok = false;
          }
          return next();
        } else {
          return next(new Error("Invalid condition type"));
        }
      };
    })(this)).then(function(err) {
      if (err || !ok) {
        return skip(err);
      } else {
        return succeed();
      }
    });
  },
  not_if_exec: function(options, skip, succeed) {
    console.log('not_if_exec is deprecated, use unless_exec');
    options.unless_exec = options.not_if_exec;
    delete options.not_if_exec;
    return each(options.unless_exec).run(function(cmd, next) {
      var run;
      if (typeof options.log === "function") {
        options.log("Mecano `unless_exec`: " + cmd);
      }
      options = {
        cmd: cmd,
        ssh: options.ssh
      };
      run = exec(options);
      if (options.stdout) {
        run.stdout.pipe(options.stdout, {
          end: false
        });
      }
      if (options.stderr) {
        run.stderr.pipe(options.stderr, {
          end: false
        });
      }
      return run.on("exit", function(code) {
        if (typeof options.log === "function") {
          options.log("Mecano `unless_exec`: code is \"" + code + "\"");
        }
        if (code === 0) {
          return skip();
        } else {
          return next();
        }
      });
    }).then(succeed);
  },
  not_if_exists: function(options, skip, succeed) {
    var destination, ssh, unless_exists;
    console.log('not_if_exists is deprecated, use unless_exists');
    options.unless_exists = options.not_if_exists;
    delete options.not_if_exists;
    ssh = options.ssh, unless_exists = options.unless_exists, destination = options.destination;
    if (typeof unless_exists === 'boolean' && destination) {
      unless_exists = unless_exists ? [destination] : null;
    }
    return each(unless_exists).run(function(unless_exists, next) {
      return fs.exists(ssh, unless_exists, function(err, exists) {
        if (exists) {
          return skip();
        } else {
          return next();
        }
      });
    }).then(succeed);
  },
  should_exist: function(options, skip, succeed) {
    return each(options.should_exist).run(function(should_exist, next) {
      return fs.exists(options.ssh, should_exist, function(err, exists) {
        if (exists) {
          return next();
        } else {
          return next(Error("File does not exist: " + should_exist));
        }
      });
    }).on('error', function(err) {
      return skip(err);
    }).on('end', succeed);
  },
  should_not_exist: function(options, skip, succeed) {
    return each(options.should_not_exist).run(function(should_not_exist, next) {
      return fs.exists(options.ssh, should_not_exist, function(err, exists) {
        if (exists) {
          return next(new Error("File does not exist: " + should_not_exist));
        } else {
          return next();
        }
      });
    }).on('error', function(err) {
      return skip(err);
    }).on('end', function() {
      return succeed();
    });
  },
  all: function(context, options, failed, succeed) {
    var i, keys, next;
    if (!((options != null) && (typeof options === 'object' && !Array.isArray(options)))) {
      return succeed();
    }
    keys = Object.keys(options);
    i = 0;
    next = function() {
      var key;
      key = keys[i++];
      if (key == null) {
        return succeed();
      }
      if (module.exports[key] == null) {
        return next();
      }
      return module.exports[key].call(context, options, function(err) {
        if (typeof options.log === "function") {
          options.log("Mecano `" + key + "`: skipping action");
        }
        return failed(err);
      }, next);
    };
    return next();
  }
};

each = require('each');

misc = require('./index');

exec = require('ssh2-exec');

fs = require('ssh2-fs');

template = require('./template');
