// Generated by CoffeeScript 1.9.1
var handler, name, register, registered, registry, wrap;

module.exports = function() {
  var call_async, call_callback, call_sync, callid, handler, jump_to_error, name, obj, properties, proto, run, stack, todos;
  if (arguments.length === 2) {
    obj = arguments[0];
    obj.options = arguments[1];
  } else if (arguments.length === 1) {
    obj = {};
    obj.options = arguments[0];
  } else {
    obj = {};
    obj.options = {};
  }
  if (obj.registry == null) {
    obj.registry = {};
  }
  properties = {};
  stack = [];
  todos = [];
  todos.err = null;
  todos.changed = false;
  todos.throw_if_error = true;
  callid = 0;
  call_callback = function(fn, args) {
    var err, mtodos, result;
    ++callid;
    stack.unshift(todos);
    todos = [];
    todos.err = null;
    todos.changed = false;
    todos.throw_if_error = true;
    try {
      result = fn.apply(obj, args);
    } catch (_error) {
      err = _error;
      todos = stack.shift();
      jump_to_error(err);
      return run();
    }
    mtodos = todos;
    todos = stack.shift();
    if (mtodos.length) {
      todos.unshift.apply(todos, mtodos);
    }
    return result;
  };
  call_sync = function(fn, args) {
    var err, mtodos, result;
    ++callid;
    stack.unshift(todos);
    todos = [];
    todos.err = null;
    todos.changed = false;
    todos.throw_if_error = true;
    try {
      result = fn.apply(obj, args);
    } catch (_error) {
      err = _error;
      todos = stack.shift();
      jump_to_error(err);
      return run();
    }
    mtodos = todos;
    todos = stack.shift();
    if (mtodos.length) {
      todos.unshift.apply(todos, mtodos);
    }
    return result;
  };
  call_async = function(fn, options, callback) {
    var err, finish, i, k, len, ref, ref1, t, v;
    if (options == null) {
      options = {};
    }
    ++callid;
    if (Array.isArray(options)) {
      for (i = 0, len = options.length; i < len; i++) {
        t = options[i];
        ref = obj.options;
        for (k in ref) {
          v = ref[k];
          if (typeof t[k] === 'undefined') {
            t[k] = obj.options[k];
          }
        }
      }
    } else if (typeof options === 'object') {
      t = options;
      ref1 = obj.options;
      for (k in ref1) {
        v = ref1[k];
        if (typeof t[k] === 'undefined') {
          t[k] = obj.options[k];
        }
      }
    }
    try {
      stack.unshift(todos);
      todos = [];
      todos.err = null;
      todos.changed = false;
      todos.throw_if_error = true;
      finish = function(err, changed) {
        if (arguments[0] == null) {
          arguments[0] = null;
        }
        if (!err) {
          arguments[1] = !!arguments[1];
        }
        if (arguments.length === 0) {
          arguments.length = 2;
        }
        if (todos.length === 0) {
          todos = stack.shift();
        }
        if (err && callback) {
          todos.throw_if_error = false;
        }
        if (err) {
          jump_to_error(err);
        }
        if (callback) {
          call_callback(callback, arguments);
        }
        if (changed && !err && !(options != null ? options.shy : void 0)) {
          todos.changed = true;
        }
        return run();
      };
      return wrap(obj, [options, finish], function(options, callback) {
        return fn.call(obj, options, callback);
      });
    } catch (_error) {
      err = _error;
      todos = stack.shift();
      jump_to_error(err);
      return run();
    }
  };
  jump_to_error = function(err) {
    while (todos[0] && todos[0][0] !== 'then') {
      todos.shift();
    }
    return todos.err = err;
  };
  run = function() {
    var changed, err, fn, ref, todo;
    todo = todos.shift();
    if (!todo) {
      if (todos.err && todos.throw_if_error) {
        throw todos.err;
      }
      return;
    }
    if (todo[0] === 'then') {
      err = todos.err, changed = todos.changed;
      todos.err = null;
      todos.changed = false;
      todos.throw_if_error = true;
      todo[1][0].call(obj, err, changed);
      run();
      return;
    }
    if (todo[0] === 'call') {
      if (todo[1][0].length === 2) {
        return call_async(todo[1][0], null, null);
      } else {
        changed = call_sync(todo[1][0], []);
        if (changed) {
          todos.changed = true;
        }
        return run();
      }
    }
    todo[1][0].user_args = ((ref = todo[1][1]) != null ? ref.length : void 0) > 2;
    fn = obj.registry[todo[0]] || registry[todo[0]];
    return call_async(fn, todo[1][0], todo[1][1]);
  };
  properties.child = {
    get: function() {
      return function() {
        return module.exports(obj.options);
      };
    }
  };
  properties.then = {
    get: function() {
      return function() {
        todos.push(['then', arguments]);
        if (todos.length === 1) {
          process.nextTick(run);
        }
        return obj;
      };
    }
  };
  properties.call = {
    get: function() {
      return function() {
        todos.push(['call', arguments]);
        process.nextTick(function() {});
        if (todos.length === 1) {
          process.nextTick(run);
        }
        return obj;
      };
    }
  };
  proto = Object.defineProperties(obj, properties);
  Object.defineProperty(obj, 'register', {
    get: function() {
      return function(name, handler) {
        var is_registered_locally;
        is_registered_locally = obj.registered(name, true);
        if (handler === null || handler === false) {
          if (is_registered_locally) {
            delete obj.registry[name];
            delete obj[name];
          } else if (module.exports.registered(name)) {
            throw Error('Unregister a global function from local context');
          }
          return obj;
        }
        if (is_registered_locally) {
          throw Error("Function already defined '" + name + "'");
        }
        obj.registry[name] = handler;
        return Object.defineProperty(obj, name, {
          configurable: true,
          get: function() {
            return function() {
              var dest, ref;
              dest = (ref = arguments[0]) != null ? ref.destination : void 0;
              todos.push([name, arguments]);
              if (todos.length === 1) {
                process.nextTick(run);
              }
              return obj;
            };
          }
        });
      };
    }
  });
  Object.defineProperty(obj, 'registered', {
    get: function() {
      return function(name, local_only) {
        var global, local;
        if (local_only == null) {
          local_only = false;
        }
        global = Object.prototype.hasOwnProperty.call(module.exports, name);
        local = Object.prototype.hasOwnProperty.call(obj, name);
        if (local_only) {
          return local;
        } else {
          return global || local;
        }
      };
    }
  });
  for (name in registry) {
    handler = registry[name];
    obj.register(name, handler);
  }
  return obj;
};

register = module.exports.register = function(name, handler) {
  if (handler === null || handler === false) {
    if (registered(name)) {
      delete registry[name];
    }
    if (registered(name)) {
      delete module.exports[name];
    }
    return module.exports;
  }
  if (registered(name)) {
    throw Error("Function already defined '" + name + "'");
  }
  if (name !== 'call') {
    registry[name] = handler;
  }
  return Object.defineProperty(module.exports, name, {
    configurable: true,
    get: function() {
      return module.exports()[name];
    }
  });
};

registered = module.exports.registered = function(name) {
  return Object.prototype.hasOwnProperty.call(module.exports, name);
};

registry = require('./misc/registry');

for (name in registry) {
  handler = registry[name];
  register(name, handler);
}

register('call', module.exports().call);

wrap = require('./misc/wrap');
