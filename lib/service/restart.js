// Generated by CoffeeScript 1.10.0
module.exports = function(options) {
  options.log({
    message: "Entering service_restart",
    level: 'DEBUG',
    module: 'mecano/lib/service/restart'
  });
  if (!options.name) {
    throw Error("Missing required option 'name'");
  }
  return this.execute({
    cmd: "service " + options.name + " restart"
  }, function(err, restarted) {
    if (err) {
      throw err;
    }
    if (restarted) {
      return options.store["mecano.service." + options.name + ".status"] = 'started';
    }
  });
};
