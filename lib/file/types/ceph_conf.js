// Generated by CoffeeScript 1.12.5
var misc, path;

module.exports = function(options) {
  options.log({
    message: "Entering file.types.ceph_conf",
    level: 'DEBUG',
    module: 'nikita/lib/file/types/ceph_conf'
  });
  if (!options.target) {
    throw Error('Missing target');
  }
  if (options.rootdir) {
    options.target = "" + (path.join(options.rootdir, options.target));
  }
  return this.file.ini({
    stringify: misc.ini.stringify,
    parse: misc.ini.parse_multi_brackets,
    escape: false
  }, options);
};

path = require('path');

misc = require('../../misc');
